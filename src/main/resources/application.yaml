server:
  port: 8190
  error:
    whitelabel:
      enabled: false

spring:
  application:
    name: northstar-notification-service
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${AUTH_SERVER_URL:http://localhost:8181/api/auth/oauth2/jwks}
  jpa:
    hibernate.ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5438}/${DB_NAME:northstar_notification_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:root}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 60000
      maximum-pool-size: 5
      initialization-fail-timeout: 0

notification:
  msg: "Welcome to NorthStar Notification Service"
  build-version: 1.0.0
  base-url: ${BASE_URL:http://localhost:8181}

info:
  app:
    name: Notification MicroService
    description: NorthStar Retail Notification Service
    version: 1.0.0

management:
  endpoints:
    web:
      exposure:
        include: "*"
  tracing:
    sampling:
      probability: 1.0

openapi:
  service:
    title: NorthStar Notification Service Docs
    version: 1.0.0
    url: ${OPENAPI_GATEWAY_URL:http://localhost:8181/api/notification}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

eureka:
  client:
    service-url:
      defaultZone: ${DISCOVERY_SERVER_URL:http://localhost:8761/eureka/}
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}
